/**
 * 2つの数値を足し算する
 *
 * @param a - 最初の数値
 * @param b - 2番目の数値
 * @returns 合計値
 *
 * @example
 * const result = add(1, 2);
 * console.log(result); // 3
 */
export function add(a: number, b: number): number {
  return a + b;
}
