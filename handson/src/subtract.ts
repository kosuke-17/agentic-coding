/**
 * 2つの数値を引き算する
 *
 * @param a - 引かれる数
 * @param b - 引く数
 * @returns 差
 *
 * @example
 * const result = subtract(5, 3);
 * console.log(result); // 2
 */
export function subtract(a: number, b: number): number {
  return a - b;
}
